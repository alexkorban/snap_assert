# Used by "mix format"
[
  inputs: ["{mix,.formatter}.exs", "{config,lib,test}/**/*.{ex,exs}"],
  line_length: 120,
  locals_without_parens: [
    snap_assert: 1,
    snap_assert: 2,
    snap_assert_raise: 1,
    snap_assert_raise: 2
  ]
]
